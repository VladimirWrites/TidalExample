dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Tidal Example"
include ':app',
        ':base:ui',
        ':search:domain', ':search:data', ':search:ui'
